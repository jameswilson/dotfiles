# Socket

When adding a dependency and the Socket MCP `depscore` tool is available, check its score. If the score is low, consider a better-supported alternative or a small local implementation. If the score is ambiguous, request experienced review. Audit actual imports as well as manifests such as `composer.json`, `pyproject.toml`, and `package.json`.
