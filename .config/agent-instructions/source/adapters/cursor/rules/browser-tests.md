# Browser tests

Check with `curl` before opening a browser when HTTP evidence is sufficient. For responsive, mobile, interaction, or visual review, use Playwright or equivalent real-browser automation rather than Cursor webview; verify real viewport sizing and browser behavior.
