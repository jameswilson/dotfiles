# Jira

Treat an unqualified ticket or issue reference as Jira unless context says otherwise. Prefer Atlassian MCP and use the `jira` CLI as a fallback. Fetch the description and complete comment history; when they conflict, treat newer comments as authoritative over older comments or the original description.
