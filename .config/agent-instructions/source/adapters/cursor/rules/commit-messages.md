# Commit messages

Follow the repository's recent commit-message style. Derive the Jira issue ID from the current branch name or recent commits and use it as the commit-message prefix. If no Jira ID is available, follow recent repository style without inventing one.
