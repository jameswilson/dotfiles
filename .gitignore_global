# Make all the patterns in this file apply to *all* projects using:
#   `git config --global core.excludesfile ~/.gitignore_global`

# Ignore operating system files.
.DS_Store
._*

# Ignore temporary and swap files
*~
*.swp
*#
*.bak

# Ignore patch files.
*.patch
*.diff
*.orig
*.rej
interdiff*.txt

# Ignore project files created by various IDEs.
*.sublime-*
*.tmproj

# Ignore tarballs and zipfiles; it's better to extract them as git has its own 
# compression algorithm.
*.tar.gz
*.gz
*.zip

# Ignore Photoshop PSD binary files.
*.psd

# Ignore SASS cache files.
.sass-cache
FirePHPCore
devel_themer
simplehtmldom
